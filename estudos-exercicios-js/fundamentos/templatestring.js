const nome = "Biel";

const concatenacao = 'Será que ' + nome + ' é?';

console.log(concatenacao);

const template = `${nome} é viado sim.`;

console.log(template);
console.log(`1 + 1 = ${1 + 1}`);

const up = texto => texto.toUpperCase();
console.log(`ei... ${up(`cuidado`)}!`);
console.log(`${nome}`);